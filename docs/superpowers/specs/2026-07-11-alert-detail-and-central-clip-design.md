# 告警详情与中央录像播放设计

右侧“近期告警”卡片支持点击展开和收起。展开内容包含目标类型、置信度、告警时间、处理状态、视觉风险等级、完整视觉分析和人工处理意见；同一时刻只展开一条，点击其他卡片切换。

左侧“历史”表的录像操作使用播放图标。`AlertHistory` 接受可选 `onPlayClip(url)` 回调，点击后把告警录像 URL 交给 `App`。`App` 在中央视频区域上方渲染固定位置的播放浮层，视频自适应中央区域，关闭后不影响原有实时视频流。

无录像文件的历史记录不显示播放按钮。右侧卡片展开不触发录像；该动作仅由历史表明确的播放按钮触发。
